# 35 - Properties, Getters & Setters

## Theory

1. What is a property in Python?
2. Why do we use getters and setters?
3. What does `@property` do?
4. What is `@property.setter`?
5. Why do we use `_age` instead of `age`?
6. What is a read-only property?
7. Difference between Java getters/setters and Python properties?
8. Can a property perform validation?
9. What happens if a setter raises an exception?
10. Why are properties considered Pythonic?

---

## Coding Questions

1. Create a Student class with an age property.
2. Create an Employee class with salary validation.
3. Create a Product class with price validation.
4. Create a Circle class with a read-only area property.
5. Create a Temperature class with a Fahrenheit property.

---

## Interview Challenge

Design a User Account class.

Requirements:

- Username
- Email
- Password

Rules:

- Email cannot be empty.
- Password must contain at least 8 characters.
- Use properties for validation.