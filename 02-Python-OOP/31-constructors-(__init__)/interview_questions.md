# 31 - Constructors (__init__) Interview Questions

## Theory

1. What is a constructor?
2. Why do we use `__init__()`?
3. When is `__init__()` called?
4. Is `__init__()` mandatory?
5. What is `self`?
6. Difference between constructor and normal method?
7. Can constructors have default arguments?
8. Can a class exist without a constructor?
9. What happens if the wrong number of arguments is passed?
10. Can one class have multiple objects using the same constructor?

---

## Coding Questions

1. Create a Student class using a constructor.
2. Create an Employee class with four attributes.
3. Create five Car objects using a constructor.
4. Create a Book class with default price.
5. Create a Laptop class with default RAM.

---

## Interview Challenge

Design a University System.

Classes:

- Student
- Teacher
- Course

Each class should use constructors to initialize data.

Create multiple objects and display their information.