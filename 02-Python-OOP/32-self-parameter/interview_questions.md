# 32 - self Parameter Interview Questions

## Theory

1. What is `self` in Python?
2. Is `self` a keyword?
3. Why is `self` required?
4. Who passes `self` automatically?
5. What happens if you remove `self` from an instance method?
6. Can you use another name instead of `self`?
7. Why is `self.name` different from `name`?
8. What is the difference between an instance variable and a local variable?
9. Explain `self` with an example.
10. How does Python internally call an instance method?

---

## Coding Questions

1. Create a Student class and print the student's name using `self`.
2. Create a Car class with a `display()` method.
3. Create a Book class and print book details.
4. Create an Employee class with `name` and `salary`.
5. Create three different objects and show that each stores different data.

---

## Interview Challenge

Design a Movie class.

Store:

- Title
- Director
- Duration
- Rating

Create five movie objects and implement a `display()` method that prints the details using `self`.