# Python JSON Interview Questions

## Theory

1. What is JSON?
2. Why is JSON widely used?
3. Difference between JSON and Python dictionary?
4. Difference between load() and loads()?
5. Difference between dump() and dumps()?
6. What Python module handles JSON?
7. What is JSONDecodeError?
8. How do you pretty-print JSON?
9. What is the JSON equivalent of None?
10. Why is JSON preferred over XML in modern APIs?

---

## Coding Questions

1. Convert a dictionary to JSON.
2. Read a JSON file.
3. Write data into a JSON file.
4. Convert JSON string into a dictionary.
5. Sort JSON keys alphabetically.
6. Pretty-print a nested JSON object.
7. Handle invalid JSON safely.
8. Merge two JSON dictionaries.
9. Count the number of keys in a JSON object.
10. Filter JSON objects based on a condition.

---

## Interview Challenge

Build a **Student Record Management System** that:

- Stores student records in a JSON file.
- Loads existing records on startup.
- Allows adding, searching, updating, and deleting students.
- Saves changes automatically.
- Handles file errors gracefully.